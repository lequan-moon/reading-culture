import React from 'react';
import { Typography, Box } from '@mui/material';

const ParentalSettings = () => {
  return (
    <Box>
      <Typography variant="h4">Parental Settings</Typography>
      {/* Add parental control settings here */}
    </Box>
  );
};

export default ParentalSettings; 